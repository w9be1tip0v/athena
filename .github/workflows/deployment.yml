name: Deploy to Vercel

on:
  push:
    branches:
      - main
      - preview
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint and Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.2.19'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Type check
        run: bun run generate:types

  # Deployment jobs commented out until GitHub Secrets are configured
  # Uncomment after setting up: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID

  # deploy-preview:
  #   name: Deploy Preview
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/preview' && false  # Disabled until secrets are configured
  #   needs: lint
  #   environment:
  #     name: preview
  #     url: ${{ steps.deploy.outputs.url }}
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Deploy to Vercel (Preview)
  #       id: deploy
  #       uses: amondnet/vercel-action@v25
  #       with:
  #         vercel-token: ${{ secrets.VERCEL_TOKEN }}
  #         github-token: ${{ secrets.GITHUB_TOKEN }}
  #         vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
  #         vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}

  # deploy-production:
  #   name: Deploy Production
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' && false  # Disabled until secrets are configured
  #   needs: lint
  #   environment:
  #     name: production
  #     url: ${{ steps.deploy.outputs.url }}
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Deploy to Vercel (Production)
  #       id: deploy
  #       uses: amondnet/vercel-action@v25
  #       with:
  #         vercel-token: ${{ secrets.VERCEL_TOKEN }}
  #         github-token: ${{ secrets.GITHUB_TOKEN }}
  #         vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
  #         vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
  #         vercel-args: '--prod'
